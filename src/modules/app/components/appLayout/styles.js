
import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(theme => ({
  heading: {
    fontSize: 25,
    fontWeight: 900
  },
  headerContainer: {
    width: '100%',
    textAlign: 'center',
    marginBottom: 15
  }
}));